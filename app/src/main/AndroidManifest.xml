<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.city_i">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Camera features (optional) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CivicConnect"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- MAIN ACTIVITY - Launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CivicConnect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN ACTIVITY - Must be exported or have intent-filter -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicConnect"
            android:windowSoftInputMode="adjustResize|stateVisible" />

        <!-- REGISTER ACTIVITY -->
        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicConnect"
            android:windowSoftInputMode="adjustResize|stateVisible" />

        <!-- FORGOT PASSWORD ACTIVITY -->
        <activity
            android:name=".auth.ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicConnect" />

        <!-- USER DASHBOARD ACTIVITY -->
        <activity
            android:name=".dashboard.UserDashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicConnect" />

        <!-- REPORT ISSUE ACTIVITY -->
        <activity
            android:name=".dashboard.ReportIssueActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicConnect"
            android:windowSoftInputMode="adjustResize|stateVisible" />

        <!-- TRACK ISSUE ACTIVITY -->
        <activity
            android:name=".tracking.TrackIssueActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicConnect" />

        <!-- MAP VIEW ACTIVITY -->
        <activity
            android:name=".tracking.MapViewActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicConnect" />

        <!-- SERVICES -->
        <service
            android:name=".services.LocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".services.NotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Firebase App Indexing Service -->
        <service
            android:name="com.google.firebase.appindexing.AppIndexingService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.appindexing.UPDATE_INDEX" />
            </intent-filter>
        </service>

        <!-- Firebase Messaging Receiver -->
        <receiver
            android:name="com.google.firebase.iid.FirebaseInstanceIdReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </receiver>

        <!-- File Provider for Camera/Image Sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>